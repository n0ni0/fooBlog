{% extends '/frontend/layout.twig' %}

{% block title %}{% endblock %}

{% block article %}
	<div id="primary-content" class="clearfix">

	<div id="aviso-formulario">
		{{ message }}
	</div>

    <form action="{{ path('contact') }}" method="post" >
    	{{ form_widget(form) }}
    	<input type="submit" name="submit" value="Enviar">
    </form>
    </div>
{% endblock %}

{% block javascripts %}
{{ parent() }}
	 <script>
    CKEDITOR.replace( 'form[message]' );
</script> 
{% endblock %}
